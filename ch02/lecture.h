#pragma once
void ShowLecture();
#include<stdio.h>

void Swap(int* a, int* b); //함수의인자에 어떤경우에 포인터를 사용할까

void IncreaseScore(int* score, int points);

void Upgradeweapon(int* weaponLV, int baseAP, float* weight);

void mytemp(int a, int b);